> ⚠️ **[이력용 · 아카이브]** 2026-04 초기 rev0 점검 기록. 현재 구현 현황은 반영하지 않음.
> 최신 구현 현황은 [`features.md`](features.md)(SSOT), 남은 이슈는 [`CHANGELOG.md`](../CHANGELOG.md)의 "알려진 이슈" 참조.

기능확인 체크리스트
(로그인)
![[Pasted image 20260410085641.png]]
1. 로그인 기능만 있는데 회원가입 버튼과 화면은 없음. 구현 필요함.
2. 비밀번호 입력란에 문자가 안보이게 되어있는데 사용자가 눈 표시를 클릭하면 문자가 보일 수 있고 다시 클릭하면 문자가 안보이게 수정

(메인프레임)
![[Pasted image 20260410084757.png]]
1. 좌측상단에 DC DCIM Manager 를 클릭하면 반응이 없음. 대시보드로 이동하게 수정
2. 상단에 텍스트 박스가 무슨의도로 만든지 모르겠음. 서버 이름을 넣으면 리스트 서버가 필터링 되는 기능일 것 같은데 입력해도 리스트 반영없음. 키보드 입력에 따라 리스트의 항목들이 필터링 되도록 수정. 그런데  리스트가 없는데 dashboard 화면에서 이 텍스트 박스가 필요한가?
3. 우측상단에 종모양으로 숫자가 보이는데 알람 메시지가 클릭하면 보이던지 해야할 것 같은데 기능 동작 안함.

(dashboard)
![[Pasted image 20260410085925.png]]
1. dashboard 상단에 tatal equipment 와 같은 4가지 블록에 너무 텍스트만 있어서 시각적 효과가 없음. 각 섹션의 색상을 다르게 하고 이미지를 더해서 각 4가지 항목들을 보여줄 수 있게 이미지를  추가.
2. Total Equipment 에 active maint 와 같은 항목들에 마우스 커서가 이동하면 그 리스트가 화면에 오버레이 되어 보일 수 있게 기능 추가.
3. 쿠버네티스를 통해서 실행되고 있는 task 정보를 추가. 어떤 네임스페이스에 몇개 서버에 어떤 워크로드를 실행 하고 있는지 정보 표시
4. 

(server)
![[Pasted image 20260410090408.png]]
1. 서버 리스트 서버를 클릭하면 세부 화면으로 전환 기능 추가
2. ip 와 status 사이에 bmcip 항목을 추가하여 표시
3. 메모리 항목을 삭제하고 대신에 cpu 제조사를 추가(intel,amd,arm 등)
4. 시스템 이름을 보여줄 수 있게 추가(SPR, GNR, EMR, TURIN, VENICE 와 같이) > 물론 서버정보에 위 항목도 없다면 추가되어어야 함.

![[Pasted image 20260410090906.png]]
1. 위 화면에서 너무 비어있는 느낌임. 이미지를 추가하고 색상도 다르게 표시
![[Pasted image 20260410091016.png]]
2. 빨간줄 노란줄 파란줄 각 시스템이 위치하고 있는 곳에 마우스 커서가 가면 어떤시스템인지 화면에 표시 기능 추가

(Alerts)
![[Pasted image 20260410092850.png]]
1. memory(4) disk(2)와 같은 항목들을 클릭하면 필터링 되도록 구현
2. 
(이외의 기능 확인)
2. 좌측아래에 세팅 버튼이 동작은 안함 화면이 없음.
3. 좌측메뉴에 Racks 가 동작을 안함 화면 전환 에러남.
4. 