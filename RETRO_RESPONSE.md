# 회고 응답 — DC Express / firstTest

작성: 2026-08-25
작성자: Grok
대상 브랜치: `claude/dcim-management-system-6oyFy` (2026-07-23)
대상 프롬프트: `RETRO_PROMPT.md`
근거 문서: `docs/retrospective-prep.md`, `docs/retrospective-summary.md`, `docs/stats.md`, `docs/features.md`, `docs/next-project-ai-workflow.md`, `CLAUDE.md`, `todo.md`, `README.md`
내부 IP·호스트명·실서버 식별값은 넣지 않음.

---

## A. 한 줄 진단

Grafana와 엑셀을 대체하는 사내 DCIM을 1인+AI로 운영 투입 직전까지 만들었고, “완성본”은 기능 목록과 발표 자료에서는 닫혔으나 테스트·리팩터·운영 훅에서는 아직 열려 있다.

## B. 모자란 것

1. **시작 문장이 제품이 아니라 장르였다.** 근거: 회고 Improve 1 — “DCIM 만들어줘”로 시작, DB·API·메트릭 구조를 1주차에 고정하지 않음. Phase 4에서 Prometheus/BMC만 78+ fix. 초기 장치: 1주차에 데이터 계약 1장(메트릭 소스, 물리 계층, 인증, 배포 금지 목록).
2. **완료 정의가 기능 수였다.** 근거: `docs/features.md` 136 ✅, `docs/stats.md` 단위테스트 72 / API 74. 기능 완료와 운영 완료가 같은 체크였다. 초기 장치: 기능 ✅ 엽에 “실기 1회 / 테스트 유무 / 롤백 방법” 세 칸.
3. **검증이 문법에 머물러다.** 근거: CLAUDE.md 검증 루트 — tsc·72테스트 통과 후 런타임 에러 반복. capacity/reports는 화면이 통째로 죽었다. 초기 장치: 페이지 추가 시 브라우저 스모크 1회를 커밋 조건.
4. **리팩터를 미봰다.** 근거: refactor 3/576. 매핑 로직 3곳 분산, PageHeader 이중 구조, 권한 체크 라우트별 제각각. 초기 장치: 마일스톤 전 `/simplify` 반나절.
5. **규칙을 말로 유지했다.** 근거: CLAUDE.md 25회 수정, 팀A/B를 매번 프롬프트로 호출. 훅은 6월 말에야 일부. 초기 장치: 세 번 같은 지시를 하면 그날 스킬/훅 파일.
6. **환경 제약을 늦게 박았다.** 근거: “파일로 push하세요” → 폐쇄망이라 손으로 타이핑. 출력 최소화 규칙은 사고 다음. 초기 장치: 1일차에 실행 환경 1페이지(반출 불가, 타이핑, git 네트워크 금지).
7. **본편과 발표가 같은 브랜치에서 동시에 자랐다.** 근거: 7월 커밋이 배포 복구와 혁신 PPT·회고 슬라이드를 오감. 추정: 후반 2주는 제품보다 설득 산출물 비중이 큼. 초기 장치: 발표 브랜치 분리, 본편 WIP 1개.

## C. 속도와 완성도를 동시에 올렸을 조건

1. 1주차에 외부 데이터 샘플을 파일로 남긴 뒤에만 API를 쓠다. 어길 시: 메트릭 Job 이름·instance 형식을 네 번 바꾼다.
2. 기능 커밋 = 코드 + 테스트 1개 + features.md 한 줄. 어길 시: API 74개에 테스트 1.4%.
3. 위험 명령은 훅으로 막는다. 어길 시: compose로 앱을 올리다 운영 DB가 내려간다.
4. 같은 에러 3회면 질문을 “이 에러”에서 “전제 조건”으로 바꾼다. 어길 시: 빌드 실패에 에이전트 12명·수시간.
5. 마일스톤마다 리팩터 반나절. 어길 시: 헤더 컴포넌트 직렬화로 화면 크래시.
6. 발표 자료는 코드 SSOT(`docs/stats.md`)만 인용한다. 어길 시: 슬라이드 숫자와 코드가 어귳난다.

## D. 고치면 다시 생기는 구멍

1. **추측 쿼리 구멍.** PromQL·Redfish를 기억으로 짤다. 장치: `/data-first-check` + `check/results/`에 샘플 1건.
2. **문법 통과 구멍.** verify는 되고 화면은 죽는다. 장치: Playwright로 해당 경로 1회, ISR 페이지는 빌드 전제 확인.
3. **권한 복사 구멍.** 어떤 라우트만 고친다. 장치: 권한 헬퍼 1곳 + 보안 회귀 테스트(이미 72개 중 일부).
4. **배포 구멍.** sh로 스크립트 실행, NFS 잔여, 깨진 `.next` 서빙. 장치: bash 재실행, 산출물 검증 후 기동, ChunkLoadError 1회 리로드.
5. **세션 망각 구멍.** 요약본이 기능을 지운다. 장치: features.md를 구현 전에 읽기. 읽지 않으면 재구현.

## E. 그때 들었으면 좋았을 코칭

DCIM은 화면이 아니라 데이터 계약이다. 실메트릭 한 줄을 보기 전에 대시보드를 그리지 마라. 기능 100개는 완성이 아니다. 폐쇄망 제약은 매너가 아니라 아키텍처다. 다음 프로젝트 첫 주 규칙: 환경 제약·데이터 샘플·위험 명령 훅 세 파일이 있기 전에는 페이지를 추가하지 말 것.

## F. 살릴 것 / 버릴 것 / 보류

| 구분 | 항목 |
|---|---|
| 살릴 것 | Data-First + check/ 체계, features.md SSOT, 팀A/B 에이전트, impact-analyzer, stats.md 자동 생성, 하이브리드 배포 문서, next-project-ai-workflow.md |
| 버릴 것 | “기능 수만 보고 완료 선언”, 단일 초대형 PR, 규칙을 채팅으로만 반복, main을 본편으로 착각하는 습관 |
| 보류 | 사내망 Claude/MCP 직접 설치, DRAM 인증 테스트 관리, 온도 외부 DB, Vault, 시리얼 콘솔 |

## G. 다음 프로젝트 10문항

1. 성공이 한 문장이고 데이터 계약이 한 장인가?
2. 실행 환경 제약(반출·네트워크·권한)이 1일차 문서인가?
3. 외부 시스템 샘플이 파일로 있는가?
4. 위험 명령 훅이 있는가?
5. 기능 커밋에 테스트가 붙는가?
6. features.md가 구현보다 앞서는가?
7. 같은 지시를 세 번 했다면 파일로 바꾸는가?
8. 본편 브랜치와 발표 브랜치가 나뉠어 있는가?
9. 화면 검증(브라우저) 계획이 있는가?
10. “완성”이 기능 수가 아닌가?

하나라도 아니면 새 페이지 코드 금지.

## H. 책용 장면

1. 4월, 메트릭을 추측으로 붙인다. 실데이터와 instance 형식이 다르다. 판단은 “일단 화면”. 틀린 점은 샘플 한 줄을 먼저 받지 않은 것. 이후 규칙 이름이 Data-First가 된다.
2. 6월 말, 용량·리포트 페이지가 “something went wrong”만 보여 준다. verify는 통과했다. 원인은 서버 컴포넌트가 아이콘 함수를 클라이언트로 넘긴 것. 판단은 스키마 드리프트로 의심. 틀린 점은 브라우저를 커밋 전에 안 열 것.
3. 6월 29일 전후, 금지라고 적어 둔 compose 명령이 운영 DB를 건드린다. 판단은 “문서에 있으니 지키겠지”. 틀린 점은 말을 훅으로 안 바꾼 것. 다음 문장의 재료가 된다: 규칙은 파일이고, 파일 다음은 강제다.

## 이 프로젝트만

1. **완성본의 재정의.**
   - 기능 완성: 거의 맞음. features 136, 페이지 39, API 74. v0.9가 그 선.
   - 운영 투입 완성: 부분. 하이브리드 배포·복구 스크립트·장애 매뉴얼은 있음. 깨진 빌드 산출물을 서빙한 사고가 7월에도 남음.
   - 문서·PPT 완성: 과함. 매뉴얼·혁신 PPT·회고 PPT·주간보고가 제품을 앞질러 닫힌 느낌.
   - 테스트 완성: 아님. 72케이스, API 테스트 1.4%, E2E 6.
   책에서 쓸 문장: “완성본처럼 보인 것은 화면과 슬라이드였고, 닫히지 않은 것은 검증과 강제였다.”

2. **세 앱과 규칙의 이동.** 여기서 배운 것을 개인 앱이 자동으로 상속하지 않았다. 레포가 바뀌면 헌법도 리셋된다.

   | 규칙 | firstTest | 세 개인 앱에서 |
   |---|---|---|
   | Data-First | 생김. 116 스크립트 | 거의 실종. 스토어·모델·광고 스펙을 1일차에 안 박음 |
   | features.md SSOT | 생김 | todo.md가 출시/백로그를 섞음 |
   | 팀A/B | 파일로 고정 | 채팅으로 가끔 |
   | 검증 루트 | 말은 있음, 훅은 늦음 | CI 신화, 골든 불일치, 테스트 후행 |
   | 폐쇄망 출력 최소화 | 고유 | 해당 없음. 대신 스토어·키스토어·라이선스가 같은 역할 |
   | WIP 1개 | 후반에도 본편+PPT | 세 앱 동시 시작으로 더 나쁠짐 |

3. **폐쇄망이 바꾼 것.** 확인 스크립트 출력을 한 줄로 줄임. 피드백 한 바퀴가 수십 분. Bulk HW Refresh 6회. 개방망이었다면 샘플 왕복·브라우저 검증·서버 pull이 달라졌을 것이고, fix 23%는 줄었을 것이다.

4. **136 기능 vs 72 테스트.** 권한 체크를 일부 라우트만 고침 → 전원/메모리 경로 우회. TranslatedPageHeader에 아이콘 함수 전달 → capacity/reports 크래시. tsc는 통과.

5. **규칙 6개는 사고를 0으로 만들지 못했다.** 같은 족의 사고를 짧게 만들었다. 팀A/B와 검증 루트는 호출을 건너뛰면 무효. 훅이 아님.

6. **책 배분.** firstTest는 독립 장 2개 + 세 앱 장에 한 줄 대조. 중복 금지: “AI가 4만 줄을 쓰다”로 장을 열지 말 것. “80%에서 고친다”는 개인 앱 장 전유. 귀여움·스토어·모델 라이선스는 여기 장에 끌지 말 것.

7. **다음 사내 템플릿.** 첫 주 복사: CLAUDE.md 규칙 섹션, `.claude/agents/`, Data-First 스킬, `docs/features.md` 빈 뼈대, `docs/next-project-ai-workflow.md`. 복사 금지: 운영 접속 정보 문서, 실데이터 `check/results/`, 발표 PPT 숫자(옛 스냅샷).

## 아직 답하지 못한 것

1. 2026-07-23 이후 현업에서 주 사용 화면은 무엇인가. 대시보드인가, 자산인가, 트윈인가.
2. v0.9를 공식 운영 버전으로 고정했는가, 계속 개발 브랜치인가.
3. compose 사고 이후 PreToolUse 훅을 실제로 넣었는가.
4. 공청회·혁신 보고 이후 조직이 다음 과제로 승인한 것이 있는가.
5. 개인 앱 세 개를 시작할 때 firstTest 헌법 중 무엇을 의도적으로 안 가져갔는가.
