# 코드/스크립트 작성 규칙

## URL 전달 규칙
- URL을 사용자에게 전달할 때 `<>` 로 감싸지 않는다
- 따옴표(`"` 또는 `'`)만 사용한다
- 예시 (올바름): `curl -s "http://10.100.175.248:8080/api/v1/query"`
- 예시 (금지): `curl -s <http://10.100.175.248:8080/api/v1/query>`

## check 스크립트 규칙
- Prometheus URL 기본값: `http://10.100.175.248:8080` (ClusterIP)
- Grafana URL: `http://10.144.38.100:30004`
- 사용자가 수동 타이핑하므로 출력량 최소화
- `YYYYMMDD-{내용}.sh` 형식으로 `check/` 폴더에 생성
